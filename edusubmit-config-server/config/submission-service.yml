server:
  port: 8082

spring:
  application:
    name: submission-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/edusubmit_submission_db

management:
  endpoints:
    web:
      exposure:
        include: health,info
